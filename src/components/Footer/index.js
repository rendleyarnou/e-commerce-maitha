import React from 'react';
import { FooterContainer } from './styles';

const Footer = () => {
    return (
        <FooterContainer>
            Todos os direitos reservados, Shoe's
        </FooterContainer>
    );
};

export default Footer;